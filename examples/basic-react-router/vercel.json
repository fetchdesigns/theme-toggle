{
  "buildCommand": "cd ../.. && pnpm build && cd examples/basic-react-router && pnpm build",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "build/client",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/server"
    }
  ]
}

